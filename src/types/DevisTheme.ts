export interface DevisTheme {
  header: {
    borderBottom: string
    devisNumber: string
  }

  boxes: {
    background: string
    border: string
    titleText: string
  }

  table: {
    headerBackground: string
    headerText: string
    rowAltBackground: string
    border: string
  }

  summary: {
    htBackground: string
    tvaBackground: string
    totalBackground: string
    totalText: string
  }

  footer: {
    borderTop: string
    text: string
  }
}
